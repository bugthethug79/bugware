# Bugware Static Site

Single-page static HTML site (`index.html`) with assets in `assets/`.

## Run
- Dev: served via workflow `Start application` using `python3 -m http.server 5000 --bind 0.0.0.0`.
- Deploy: static, publicDir = `.`.
